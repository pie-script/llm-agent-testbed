[project]
name = "llm-agent-testbed"
version = "0.1.0"
description = "A cybersecurity testbed for evaluating LLM agent vulnerabilities"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "google-genai>=2.19.0",
    "python-dotenv>=1.2.3",
]
